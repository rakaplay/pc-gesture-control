---
## На русском!

`rakaplay/pc-gesture-control` - мой пет-проект, который был создан на базе Mediapipe. Он позволяет управлять компьютером жестами.

## Какие жесты доступны?
Доступны:
- Клик левой кнопкой мыши через щипок 🤏
- Клик правой кнопкой мыши через смыкание безымянного и большого пальцев
- Клик Alt+Tab при смыкании мизинца и большого пальца
- Скролл при смыкании среднего и указательного пальцев

## Почему может плохо работать?
Потому что надо базовое расстояние настроить, либо инверсию по x/y включить.
Также, рекомендую использовать сайт (https://rakaplay.github.io), так как тут еще надо пофиксить код сайта.

## Как запустить?

Реализовал через сервер-клиентскую архитектуру. Клиент в браузере берет картинку с вебки и распознает жесты (через версию медиапайпа на javascript), а сервер получает данные от клиента по вебсокету и перемещает курсор. Браузер же не может двигать курсором, так бы и сервера не было бы.

Чтобы скачать сервер, сначала сделайте venv, затем скачайте библиотеки из папки server, они лежат в файле requirements.txt, и можно скачать их через pip примерно вот так:
```bash 
pip install -r requirements.txt 
```
Запускаете main.py в папке server. Он требует интернета, так как я... не добавил кэширование файлов. Так что, со включенной сетью, все заработает. Проверка идет через пинг на google.com. 
Оставил легаси-версию сервера в server-monolith.py. Лучше не запускать, дизайн у нее так себе.

После запуска, сервер пишет статус подключения. 

Если открыть клиент (rakaplay.github.io) в браузере, то статус у сервера автоматически обновится, и можно включать веб-камеру в клиенте и пользоваться! Но надо подождать, пока в браузере медиапайп загрузится.

## Минималки?

Ну, скорее всего, надо видеокарту хоть какую-то. Обрабатывать картинку в 30 кадров, даже хоть в 320*240 - для процессора будет трудно. Так что, видеокарта желательна. Либо же процессор с поддержкой SIMD (одна инструкция, много данных). По оперативной памяти все неплохо, вкладка потребляет около 200 Мб, что для такого функционала, в принципе, класс.

Вот такие пироги.

Ну и из-за лицензии, укажу: 
Google Mediapipe is licensed under Apache 2.0.
JavaScript is a trademark of Oracle and/or its affiliates.